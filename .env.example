# --- Database (Vercel Postgres / Neon) ---
# Lấy 2 giá trị này trong Vercel Dashboard > Storage > (database) > .env.local tab
POSTGRES_PRISMA_URL="postgres://user:password@host/db?pgbouncer=true&connect_timeout=15"
POSTGRES_URL_NON_POOLING="postgres://user:password@host/db"

# --- Auth.js ---
# Tạo bằng lệnh: npx auth secret
AUTH_SECRET="dan-mot-chuoi-random-o-day"

# URL của site (dùng khi deploy). Ở local để mặc định.
# AUTH_URL="https://luyentiengtrung.com"
